{% extends 'front/professional/layout.html.twig' %}

{% block title %}{{ 'Nouveaux professionnels - '~ parent() }}{% endblock %}

{% block pro_breadcrumb %}
    {% embed "front/partials/breadcrumb.html.twig" %}
        {% block breadcrumb_heading %}
            <div class="section-heading">
                <h2 class="sec__title text-white font-size-40 mb-0">Nouveaux professionnels</h2>
            </div>
        {% endblock %}
        {% block breadcrumb_links %}
            <li>Nouveaux professionnels</li>
        {% endblock %}
    {% endembed %}
{% endblock %}

{% block pro_content %}
    <section class="card-area section-padding">
        <div class="container">
            {% if professionals|length > 0 %}
                <div class="row">
                    <div class="col-lg-12">
                        <div class="filter-bar d-flex flex-wrap justify-content-between align-items-center margin-bottom-30px">
                            <p class="result-text font-weight-medium">Affichage de 1 à {{ professionals|length }} sur {{ my_app.professionalsNew|length }} entrées</p>
                            <div class="filter-bar-action d-flex flex-wrap align-items-center">
                                <a href="#" class="search-filter" data-toggle="modal" data-target="#searchFilterModal">
                                    <i class="la la-sliders mr-1"></i>Recherche détaillée
                                </a>
                                <div class="user-chosen-select-container ml-3">
                                    <select class="user-chosen-select">
                                        <option value="sort-by-default">Trier par défaut</option>
                                        <option value="high-rated">High Rated</option>
                                        <option value="most-reviewed">Most Reviewed</option>
                                        <option value="popular-Listing">Popular Listing</option>
                                        <option value="newest-Listing">Newest Listing</option>
                                        <option value="older-Listing">Older Listing</option>
                                        <option value="price-low-to-high">Price: low to high</option>
                                        <option value="price-high-to-low">Price: high to low</option>
                                        <option value="all-listings">Random</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    {% for professional in professionals %}
                        {% embed "front/partials/professional/box.html.twig" with {'professional' : professional} %}{% endembed %}
                    {% endfor %}
                </div>
                <div class="row">
                    <div class="col-lg-12 pt-3 text-center">
                        {% do professionals.setPageRange(4) %}
                        {{ knp_pagination_render(professionals, 'front/partials/pagination.html.twig') }}   
                    </div>
                </div>
            {% else %}
                <div class="row">
                    <div class="col-lg-12 pt-3 text-center">
                        Aucune donnée à afficher pour le moment, merci de bien vouloir revenir plutard.  
                    </div>
                </div>
            {% endif %}
        </div>
    </section>
{% endblock %}