<div class="modal fade modal-container message-form" id="messageModal" tabindex="-1" role="dialog" aria-labelledby="messageModalTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header align-items-center mh-bg-2">
                <h5 class="modal-title" id="messageModalTitle">{{'global.send_a_message'|trans}}</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true" class="la la-times-circle"></span>
                </button>
            </div>
            <div class="modal-body">
                <p class="font-size-15 font-weight-medium pb-2">
                    <span class="text-color">À :</span> {{ name }}
                </p>
                <form method="post" class="form-box">
                    <div class="input-box">
                        <label class="label-text">Partagez quelques détails afin que nous puissions vous mettre en contact avec le freelance</label>
                        <div class="form-group">
                            <span class="la la-pencil form-icon"></span>
                            <textarea class="message-control form-control" name="message" placeholder="Ecrivez quelque chose..."></textarea>
                        </div>
                    </div>
                    <div class="input-box">
                        <label class="label-text d-block">Inclure vos informations
                            <span class="text-gray">{{ 'global.or'|trans }}</span> <a class="text-color-2" href="{{ path('app_login') }}">Connectez vous</a>
                        </label>
                        <label class="label-text">{{ 'global.email_address'|trans }}</label>
                        <div class="form-group">
                            <span class="la la-envelope-o form-icon"></span>
                            <input value="{{ app.user ? app.user.email : '' }}" class="form-control form-control-styled" type="email" name="email" placeholder="Email">
                        </div>
                    </div>
                    <div class="input-box">
                        <label class="label-text">Nom</label>
                        <div class="form-group">
                            <span class="la la-user form-icon"></span>
                            <input value="{{ app.user ?? '' }}" class="form-control form-control-styled" type="text" name="text" placeholder="Votre nom">
                        </div>
                    </div>
                    <div class="btn-box">
                        <p class="font-size-13 font-weight-medium pb-2 line-height-16">Nous enverrons vos informations à l'entreprise pour vous aider à obtenir une réponse.</p>
                        <button type="submit" class="theme-btn gradient-btn w-100">
                            {{'global.send_a_message'|trans}}<i class="la la-arrow-right ml-2"></i>
                        </button>
                        <p class="font-size-13 font-weight-medium pt-1">En poursuivant, vous acceptez notre
                            <a href="#" class="text-color-2">Conditions d'utilisation</a> et
                            <a href="#"  class="text-color-2">Politique de confidentialité</a>.
                        </p>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>